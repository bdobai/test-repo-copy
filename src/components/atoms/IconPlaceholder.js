import React from 'react'
import { View } from 'react-native'
import { scaleSize } from '_styles/mixins'

const IconPlaceholder = props => {
    return <View style={{width: scaleSize(48)}}></View>
}

export default IconPlaceholder
